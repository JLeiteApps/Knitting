{
  "name": "knitting",
  "version": "1.0.0",
  "private": true,
  "description": "Knitting pattern adaptation: knowledge base + deterministic modification engine + web/app",
  "license": "ISC",
  "type": "commonjs",
  "workspaces": [
    "app/packages/*",
    "app/apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p app/packages/schema/tsconfig.json --noEmit && tsc -p app/packages/shared/tsconfig.json --noEmit && tsc -p app/packages/parser/tsconfig.json --noEmit && tsc -p app/packages/engine/tsconfig.json --noEmit && npm run typecheck --workspace @knitting/web",
    "dev:web": "npm run dev --workspace @knitting/web",
    "build:web": "npm run build --workspace @knitting/web"
  },
  "devDependencies": {
    "@types/react-test-renderer": "^19.1.0",
    "fake-indexeddb": "^6.2.5",
    "react-test-renderer": "^19.2.8",
    "typescript": "^5.6.0",
    "vitest": "^4.1.11"
  },
  "allowScripts": {
    "canvas@3.2.3": true,
    "tesseract.js@7.0.0": true,
    "esbuild@0.21.5": true
  }
}
